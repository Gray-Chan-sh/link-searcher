name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    name: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
          - platform: ubuntu-latest
            target: x86_64-unknown-linux-gnu

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libjavascriptcoregtk-4.1-dev \
            libxdo-dev \
            libappindicator3-dev \
            librsvg2-dev \
            libpipewire-0.3-dev \
            libspa-0.2-dev \
            libgbm-dev \
            patchelf

      # 前端构建：tauri::generate_context!() 编译期要求 ../dist 存在
      #（frontendDist 指向前端产物），cargo test/build 前必须先生成。
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: npm ci
        run: npm ci

      - name: Build frontend
        run: npm run build

      # Rust lib 单测：核心逻辑（检索/AI/分块/提取）跨平台验证。
      # 平台 cfg 分支（Apple Vision / Windows OCR / poppler 路径）在各 runner 上真实编译。
      - name: Cargo test (lib)
        working-directory: src-tauri
        run: cargo test --lib --target ${{ matrix.target }}

      # 全量编译：验证所有平台特定依赖 + tauri 打包链路能编译（不打 release 包）
      - name: Cargo build (debug)
        working-directory: src-tauri
        run: cargo build --target ${{ matrix.target }}
