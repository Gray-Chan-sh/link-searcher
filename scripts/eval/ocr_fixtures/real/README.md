#
# Real OCR samples — manually added, never committed.
#
# How to add:
# 1. Place PNG/JPG images in this directory with descriptive filenames.
# 2. Create a matching entry in `../synthetic/manifest.jsonl` (or a separate
#    real-specific manifest) with `max_cer` set appropriately.
#
# Privacy rule:
# Do NOT commit real samples to git. They may contain personally
# identifiable information or confidential business documents. This
# directory is fully gitignored.
#
