# Link-Searcher 用户手册

> 跨平台桌面全文搜索软件 — 快速索引、搜索您的本地文档

---

## 目录

1. [产品简介](#1-产品简介)
2. [功能特性](#2-功能特性)
3. [系统要求](#3-系统要求)
4. [安装指南](#4-安装指南)
5. [快速上手](#5-快速上手)
6. [界面说明](#6-界面说明)
7. [搜索功能详解](#7-搜索功能详解)
8. [资料库管理](#8-资料库管理)
9. [索引管理](#9-索引管理)
10. [OCR 识别](#10-ocr-识别)
11. [设置选项](#11-设置选项)
12. [数据备份与恢复](#12-数据备份与恢复)
13. [常见问题](#13-常见问题)
14. [技术栈](#14-技术栈)

---

## 1. 产品简介

Link-Searcher 是一款**跨平台桌面全文搜索**软件，支持 Windows、Linux 和 macOS 三大操作系统。它能快速索引您电脑中的文档，并提供毫秒级的全文搜索体验。

与传统的文件名搜索不同，Link-Searcher 可以**搜索文件内容**——即使您不记得文件名，只要记得文档中的某个词、某句话，就能找到它。

本软件功能参考 [fulltext-search](https://github.com/Gray-Chan-sh/fulltext-search) 项目，但以原生桌面应用的形式运行，无需 Docker 或浏览器。

---

## 2. 功能特性

### 核心功能

| 功能 | 说明 |
|------|------|
| **全文搜索** | 基于 Tantivy 搜索引擎，BM25 相关性排序，毫秒级响应 |
| **中文分词** | 集成 jieba 分词，支持中文搜索、精确匹配 |
| **模糊搜索** | 自动纠错，容错距离 1，如 `finantial` → `financial` |
| **查询语法** | 通配符 `doc*`，短语 `"dog cat"`，布尔运算符 `AND/OR/NOT` |
| **文件名搜索** | 使用 `filename:report.pdf` 语法按文件名搜索 |
| **搜索建议** | 输入关键词时自动补全，快速定位目标 |
| **目录树筛选** | 展开任意层级子目录，支持父级联动选择 |
| **多格式支持** | PDF、Word(docx)、Excel(xlsx)、PPT(pptx)、纯文本、代码、图片 OCR |
| **OCR 引擎管理** | 支持 Apple Vision / Windows OCR / Tesseract，引擎测试与切换 |
| **实时文件监控** | 文件新增/修改/删除自动检测，增量更新索引 |
| **错误分类 + Fallback 链** | 索引失败时自动降级，错误分类显示 |

### 界面特性

| 特性 | 说明 |
|------|------|
| **暗色模式** | 支持浅色/深色/跟随系统三种主题，平滑过渡动画 |
| **搜索结果预览** | 侧边栏预览文件内容，搜索词高亮，匹配上下导航 |
| **图片预览** | 搜索结果中直接显示图片缩略图与预览 |
| **虚拟滚动** | 万级搜索结果不卡顿，流畅滚动 |
| **键盘导航** | 方向键上下选择，Enter 打开预览 |
| **右键菜单** | 文件上右键快速打开、复制路径、打开所在文件夹 |
| **拖拽添加目录** | 直接将文件夹拖入窗口即可添加为资料库 |
| **日志查看** | 按类型筛选日志，自动刷新，便于排查问题 |
| **首次使用引导** | 三步向导，帮助新用户快速上手 |
| **骨架屏加载** | 内容加载时显示骨架屏，减少等待焦虑 |

### 系统特性

| 特性 | 说明 |
|------|------|
| **全局快捷键** | 默认 Ctrl+Space，全局呼出/隐藏搜索窗口 |
| **系统托盘** | 关闭窗口后最小化到托盘，后台持续运行 |
| **开机自启** | 一键设置开机自动启动 |
| **命令行搜索** | `link-searcher search "keyword"` 终端直接搜索 |
| **索引健康检查** | 自动检测索引完整性，异常时提示重建 |
| **搜索结果导出** | 搜索结果一键导出为 CSV 文件 |

### 部署特性

| 特性 | 说明 |
|------|------|
| **原生桌面** | 独立窗口运行，无需浏览器、无需 Docker |
| **跨平台** | Windows / Linux / macOS |
| **轻量** | 安装包约 8-15MB，内存占用约 65MB |
| **数据本地化** | 所有数据存储在本地，无需联网，隐私安全 |

---

## 3. 系统要求

### Windows

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 或更高版本 |
| WebView | WebView2（Windows 10 内置，Win10 以下需安装） |
| 内存 | 建议 2GB 以上 |
| 硬盘 | 500MB 可用空间（索引数据另计） |
| 可选 | Tesseract OCR（如需 OCR 功能） |

### macOS

| 项目 | 要求 |
|------|------|
| 操作系统 | macOS 10.15 (Catalina) 或更高版本 |
| 内存 | 建议 2GB 以上 |
| 硬盘 | 500MB 可用空间（索引数据另计） |
| 可选 | Tesseract OCR（如需 OCR 功能） |

### Linux

| 项目 | 要求 |
|------|------|
| 操作系统 | 主流发行版（Ubuntu 20.04+, Debian 11+, Fedora 36+ 等） |
| 依赖 | WebKit2GTK 4.1+，libappindicator |
| 内存 | 建议 2GB 以上 |
| 硬盘 | 500MB 可用空间（索引数据另计） |
| 可选 | Tesseract OCR（如需 OCR 功能） |

---

## 4. 安装指南

> ⚠️ **当前状态**：本项目仍在开发阶段，暂未提供预编译的安装包（.dmg/.exe/.AppImage）。
> 目前需要通过源码构建，跟着下面的步骤走就行。

### 4.1 快速启动（如果你已经有项目文件夹）

如果您已经拿到了项目文件夹，打开终端，进入项目目录后，依次执行以下 2 条命令：

```bash
# 第一步：安装前端依赖（只需执行一次，耗时约 1 分钟）
npm install

# 第二步：启动应用（会弹出桌面窗口）
npm run tauri dev
```

### 4.2 从零开始一步步安装

如果您是第一次接触这个项目，按照以下步骤操作：

#### 第一步：安装 Node.js

Node.js 是运行这个项目必需的环境。

1. 打开浏览器，访问 https://nodejs.org
2. 下载左侧的 **LTS 版本**（长期支持版，推荐给大多数用户）
3. 双击下载好的安装包，一路点「下一步」完成安装
4. 验证安装：打开终端，输入 `node --version` 并回车，看到版本号即成功

#### 第二步：获取项目代码

```bash
# 下载项目（如果已经有项目文件夹，跳过这一步）
git clone <repo-url> link-searcher
cd link-searcher
```

#### 第三步：安装并启动

```bash
# 安装前端依赖（首次运行需要，耗时约 1 分钟）
npm install

# 启动应用（会弹出一个桌面窗口）
npm run tauri dev
```

首次启动时会自动编译 Rust 后端，第一次需要 2-5 分钟，之后启动就很快了。

#### 第四步（可选）：打包成安装包

如果想生成可双击安装的安装文件：

```bash
npm run tauri build
```

构建完成后，安装包在 `src-tauri/target/release/bundle/` 目录下：
- **macOS** → `.dmg` 文件（双击安装）
- **Windows** → `.msi` 文件（双击安装）
- **Linux** → `.deb` 或 `.AppImage` 文件

### 4.3 安装 OCR 识别功能（可选）

如果需要搜索图片和扫描件中的文字，需要额外安装 Tesseract：

**macOS：**
```bash
brew install tesseract
brew install tesseract-lang
```

**Windows：**
```bash
winget install Tesseract-OCR
```
或者从 https://github.com/UB-Mannheim/tesseract/wiki 下载安装

**Linux（Ubuntu/Debian）：**
```bash
sudo apt install tesseract-ocr
sudo apt install tesseract-ocr-chi-sim tesseract-ocr-jpn tesseract-ocr-kor
```

安装后在应用「设置」页面可以查看 OCR 状态。

---

## 5. 快速上手

### 第一步：添加资料库

1. 启动 Link-Searcher
2. 点击左侧导航栏的「资料库」
3. 点击「添加目录」按钮
4. 在弹出的文件对话框中选择您要搜索的文件夹
5. 设置别名（可选，便于识别）

### 第二步：构建索引

1. 点击左侧导航栏的「索引状态」
2. 点击「开始扫描」按钮
3. 等待扫描完成（扫描时间取决于文件数量和大小）
4. 扫描完成后，索引状态会显示已索引的文件数

### 第三步：搜索文件

1. 点击左侧导航栏的「搜索」
2. 在搜索框中输入关键词
3. 按回车或点击搜索图标
4. 结果实时显示，点击任意结果可预览内容

> **提示**：首次使用建议先添加一个小文件夹（如 `~/Documents`）体验，熟悉后再添加更多目录。

---

## 6. 界面说明

```
┌─────────────────────────────────────────────────────────┐
│  Link-Searcher                                    _ □ × │
├──────────┬──────────────────────────────────────────────┤
│          │                                              │
│  🔍 搜索 │  搜索框 [输入关键词...]  🔍                   │
│          │  ┌──────────────────────────────────────────┐ │
│  📁 资料库│  │ 筛选: □ 目录A  □ 目录B  □ .pdf  □ .docx│ │
│          │  └──────────────────────────────────────────┘ │
│  📊 索引  │                                              │
│          │  ┌──────────────────────────────────────────┐ │
│  ⚙️ 设置  │  │  report.pdf                             │ │
│          │  │  /Documents/report.pdf                   │ │
│          │  │  ...年度**财务**报告显示...                │ │
│          │  │  得分: 0.95  修改时间: 2025-01-15        │ │
│          │  ├──────────────────────────────────────────┤ │
│          │  │  notes.txt                               │ │
│          │  │  ...会议**财务**预算讨论...                │ │
│          │  └──────────────────────────────────────────┘ │
│          │                                   第 1 页/共 5 页│
├──────────┴──────────────────────────────────────────────┤
│  已索引 1,234 个文件 │ 上次扫描: 2025-01-15 10:30       │
└─────────────────────────────────────────────────────────┘
```

### 导航栏

| 图标 | 页面 | 功能 |
|------|------|------|
| 🔍 搜索 | 搜索页面 | 全文搜索、结果预览、筛选 |
| 📁 资料库 | 目录管理 | 添加/删除/配置监控目录 |
| 📊 索引 | 索引状态 | 查看索引进度、扫描、重建 |
| ⚙️ 设置 | 设置页面 | 配置 OCR、定时扫描、排除模式 |

### 状态栏

底部状态栏显示：
- 已索引文件总数
- 上次扫描时间
- 当前扫描进度（正在扫描时）

---

## 7. 搜索功能详解

### 基本搜索

在搜索框中输入关键词，按回车即搜索。支持：
- **中文搜索**：`财务报表`
- **英文搜索**：`financial report`
- **混合搜索**：`2024 财务报告`
- **短语搜索**：`"年度财务报告"`

### 高级筛选

#### 目录筛选
勾选左侧目录列表，将搜索范围限定在特定目录中。

#### 文件类型筛选
勾选文件扩展名，仅搜索特定类型的文件（如仅搜索 PDF）。

#### 日期范围
设置文件修改时间范围，筛选特定时间段内的文件。

#### 排序方式

| 排序 | 说明 |
|------|------|
| 相关性 | 按 BM25 评分排序（默认） |
| 修改时间 | 按文件最后修改时间排序 |
| 文件大小 | 按文件大小排序 |
| 文件名 | 按文件名排序 |

### 搜索结果

每条结果展示：
- **文件名**：文件名称
- **路径**：文件所在完整路径
- **摘要**：匹配关键词的上下文片段，关键词高亮
- **得分**：相关性评分（0–1）
- **修改时间**：文件最后修改时间
- **文件大小**：文件大小

### 预览面板

点击搜索结果，右侧预览面板显示：
- **文件信息**：文件名、路径、大小、修改时间、MD5
- **文本内容**：文件提取的纯文本内容，关键词高亮
- **打开文件**：点击按钮用系统默认程序打开文件

### 搜索建议

输入关键词时，搜索框下方自动弹出建议列表，帮助快速定位目标。

### 模糊搜索

模糊搜索能自动纠正拼写错误，容错距离为 1（即允许一个字符的差异）。

**示例：**
- `finantial` → 匹配到 `financial`
- `acount` → 匹配到 `account`
- `recieve` → 匹配到 `receive`

模糊搜索适用于中英文混合场景，可在搜索框右侧开关。开启后系统会自动为您的输入生成纠错候选，并返回匹配结果。

### 查询语法

支持丰富的查询语法，精确控制搜索范围：

| 语法 | 示例 | 说明 |
|------|------|------|
| **通配符** | `doc*` | 匹配以 `doc` 开头的任意词 |
| **短语** | `"dog cat"` | 精确匹配短语，词序固定 |
| **AND** | `cat AND dog` | 同时包含两个词 |
| **OR** | `cat OR dog` | 包含任一词 |
| **NOT** | `cat NOT dog` | 包含 `cat` 但不包含 `dog` |
| **组合** | `(cat OR dog) AND food` | 括号分组，优先级控制 |

通配符仅支持后缀匹配（`前缀*`），不支持前缀通配符（`*后缀`）。

### 文件名搜索

使用 `filename:` 语法，将搜索范围限定在文件名上：

**示例：**
- `filename:report.pdf` — 搜索文件名为 `report.pdf` 的文件
- `filename:2024` — 搜索文件名包含 `2024` 的文件
- `cat filename:note` — 同时搜索内容含 `cat` 且文件名含 `note` 的文件

### 目录树筛选

筛选面板中的目录树支持展开任意层级子目录，帮助精确限定搜索范围。

**交互方式：**
- **展开/折叠**：点击目录前的箭头展开或折叠子目录
- **父级联动**：勾选父目录时，自动勾选所有子目录
- **三种 checkbox 状态**：
  - ✅ **全选**：该目录及其所有子目录被选中
  - ◻️ **部分选中**：目录下部分子目录被选中（灰色方块）
  - ⬜ **未选**：该目录未被选中

**示例：** 勾选 `Documents/工作` 则仅搜索该目录下的文件，`Documents` 显示为部分选中状态。

---

## 8. 资料库管理

### 添加目录

1. 进入「资料库」页面
2. 点击「添加目录」
3. 选择文件夹
4. 设置别名（可选）

### 目录配置

每个目录可以单独配置：

| 配置项 | 说明 |
|--------|------|
| 别名 | 显示名称，便于识别 |
| OCR 语言 | 该目录使用的 OCR 语言 |
| 排除模式 | Glob 模式，排除不需要索引的文件 |
| 包含类型 | 仅索引指定扩展名的文件 |
| 递归 | 是否包含子目录 |

### 排除模式示例

```
# 排除 node_modules 目录
node_modules/*

# 排除所有 .git 目录
.git/*

# 排除临时文件
*.tmp
*.log

# 排除特定文件夹
build/*
dist/*
target/*
```

### 删除目录

1. 在资料库列表中找到目标目录
2. 点击右侧的删除按钮
3. 确认删除（目录下的文件索引也会被移除）

---

## 9. 索引管理

### 索引状态页面

显示当前索引的整体状态：

| 指标 | 说明 |
|------|------|
| 文件总数 | 监控中的文件总数 |
| 已索引 | 成功索引的文件数 |
| 待处理 | 等待索引的文件数 |
| 失败 | 索引失败的文件数 |
| 上次扫描 | 最近一次全量扫描时间 |
| 扫描状态 | 当前是否正在扫描 |

### 手动扫描

- **增量扫描**：仅扫描新增或修改的文件，速度较快
- **全量扫描**：重新扫描所有文件，耗时较长
- **重建索引**：清空索引后重新构建，解决索引损坏问题

### 自动扫描

在设置中配置定时扫描时间，系统会在指定时间自动执行全量扫描。

### 扫描进度

扫描过程中，状态栏和索引页面会显示实时进度：
- 已扫描文件数
- 总文件数
- 当前正在处理的文件

---

## 10. OCR 识别

### 什么是 OCR

OCR（光学字符识别）可以将图片和扫描件中的文字提取出来，使其可以被搜索到。

### 支持引擎

Link-Searcher 支持三种 OCR 引擎，您可以根据操作系统选择：

| 引擎 | 支持平台 | 特点 |
|------|----------|------|
| **Apple Vision** | macOS | 系统原生，无需额外安装，速度快，识别质量高 |
| **Windows OCR** | Windows | 系统原生，无需额外安装，支持多语言 |
| **Tesseract** | Windows / Linux / macOS | 开源引擎，需手动安装，支持 100+ 语言 |

### 引擎选择与测试

1. 进入「设置」→「OCR 设置」
2. 从下拉菜单中选择 OCR 引擎
3. 点击「测试引擎」按钮，系统会生成一张测试图片并尝试识别
4. 测试通过后，引擎状态显示为绿色
5. **索引前必须通过引擎测试**，未测试通过的引擎不允许用于索引

### 平台安装引导

#### Apple Vision（macOS）
无需安装，macOS 系统自带。选择后即可使用。

#### Windows OCR（Windows）
无需安装，Windows 系统自带。选择后即可使用。

#### Tesseract（全平台）

**macOS：**
```bash
brew install tesseract
brew install tesseract-lang
```

**Windows：**
```bash
winget install Tesseract-OCR
```
或从 https://github.com/UB-Mannheim/tesseract/wiki 下载安装

**Linux（Ubuntu/Debian）：**
```bash
sudo apt install tesseract-ocr
sudo apt install tesseract-ocr-chi-sim tesseract-ocr-jpn tesseract-ocr-kor
```

### 支持的图片格式

PNG、JPG/JPEG、GIF、BMP、WebP、TIFF

### 扫描件 PDF

对于扫描件 PDF（即纯图片构成的 PDF），系统会自动检测：
1. 先尝试提取 PDF 中的文本
2. 如果文本极少（< 50 字符），自动调用 OCR 识别
3. OCR 结果自动编入索引

### OCR 性能建议

- OCR 处理较慢，建议先让索引建立完成后再启用
- 大量图片文件时，建议降低 OCR 并发数
- 低内存设备建议 OCR 并发数设为 1

---

## 11. 设置选项

### 常规设置

| 设置项 | 说明 |
|--------|------|
| 主题 | 浅色 / 深色 / 跟随系统 |
| 最大搜索结果数 | 单次搜索返回的最大结果数量（默认 100） |
| 开机自启 | 是否随系统启动自动运行 |
| 全局快捷键 | 自定义全局呼出/隐藏快捷键（默认 Ctrl+Space） |

### OCR 设置

| 设置项 | 说明 |
|--------|------|
| OCR 引擎 | 选择 OCR 引擎（Apple Vision / Windows OCR / Tesseract） |
| OCR 语言 | 识别文字的语言 |
| OCR 并发数 | 同时处理的 OCR 任务数（根据内存调整） |

### 扫描设置

| 设置项 | 说明 |
|--------|------|
| 定时扫描 | 每日自动全量扫描的时间 |
| 排除模式 | 全局排除规则（Glob 模式） |

### 备份设置

| 设置项 | 说明 |
|--------|------|
| 自动备份 | 启用/禁用定期自动备份 |
| 备份间隔 | 多少天自动备份一次 |

---

## 12. 数据备份与恢复

### 数据存储位置

所有数据存储在 `~/.link-searcher/` 目录下：

```
~/.link-searcher/
├── data.db          # SQLite 数据库（文件追踪、设置、搜索历史）
└── index/           # Tantivy 搜索索引目录
    ├── meta.json
    └── ...
```

### 手动备份

在「索引状态」页面点击「备份」按钮，系统会：
1. 复制索引目录到备份位置
2. 复制 SQLite 数据库
3. 备份文件存放在 `~/.link-searcher/backups/` 目录

### 恢复数据

**注意：恢复操作会覆盖当前索引数据，请谨慎操作。**

1. 关闭 Link-Searcher
2. 将备份数据复制回 `~/.link-searcher/`
3. 重新启动 Link-Searcher

### 数据迁移

要将索引数据迁移到另一台电脑：

```bash
# 在旧电脑上打包数据
tar -czf link-searcher-data.tar.gz ~/.link-searcher/

# 复制到新电脑
scp link-searcher-data.tar.gz user@new-computer:~/

# 在新电脑上解压
tar -xzf link-searcher-data.tar.gz -C ~/
```

---

## 13. 常见问题

### Q: 搜索速度如何？

对于 10 万个文档，搜索响应时间通常在 **毫秒级**（< 50ms）。首次索引构建后，后续增量索引非常快。

### Q: 支持哪些文件格式？

| 格式 | 扩展名 | 说明 |
|------|--------|------|
| 纯文本 | `.txt` `.md` `.csv` `.json` `.xml` `.yaml` `.toml` `.ini` `.log` | 直接读取 |
| 代码 | `.py` `.rs` `.ts` `.js` `.html` `.css` `.sql` `.sh` `.bat` | 作为文本处理 |
| PDF | `.pdf` | 文本提取，扫描件自动 OCR |
| Word | `.docx` | 解析 XML 提取文本 |
| Excel | `.xlsx` | 读取所有单元格 |
| PPT | `.pptx` | 解析幻灯片文本 |
| 图片 | `.png` `.jpg` `.jpeg` `.gif` `.bmp` `.webp` `.tiff` | 需 OCR 支持 |

### Q: 为什么我的文件没有被索引？

可能的原因：
1. 文件格式不受支持（见上表）
2. 文件被排除模式匹配
3. 文件权限不足
4. OCR 文件需要先安装 Tesseract

### Q: 索引占用多少磁盘空间？

索引大小通常为原始文档的 **2%–25%**。例如 1GB 的文档集，索引大约 20–250MB。

### Q: 如何重新索引所有文件？

在「索引状态」页面点击「重建索引」。

### Q: 软件会消耗多少内存？

- **空闲时**：约 65MB
- **扫描时**：约 100–200MB（取决于 OCR 并发数）
- **搜索时**：约 80–120MB

### Q: 如何让软件开机自启？

**macOS：**
系统设置 → 通用 → 登录项 → 添加 Link-Searcher

**Windows：**
任务管理器 → 启动 → 启用 Link-Searcher

**Linux：**
在 `~/.config/autostart/` 创建 `.desktop` 文件

### Q: 搜索不到中文内容？

确保：
1. 文件编码为 UTF-8（大部分现代文件都是）
2. 索引已成功建立（检查索引状态页面）
3. 中文分词正常工作（jieba 已内置）

### Q: 如何卸载？

**macOS：** 删除 `.app` 文件，然后删除 `~/.link-searcher/` 目录
**Windows：** 在「添加或删除程序」中卸载，然后删除 `%USERPROFILE%\.link-searcher\`
**Linux：** 使用包管理器卸载，然后删除 `~/.link-searcher/`

---

## 14. 技术栈

| 层 | 技术选型 |
|-----|----------|
| 桌面框架 | Tauri 2.x |
| 前端 | React 19 + TypeScript + Tailwind CSS 4 |
| 搜索引擎 | Tantivy (Rust 原生) |
| 数据库 | SQLite (rusqlite + r2d2 连接池) |
| 中文分词 | jieba-rs |
| 文本提取 | lopdf (PDF) / calamine (XLSX) / quick-xml (DOCX/PPTX) / pdf-extract |
| OCR | Apple Vision (macOS) / Windows OCR / Tesseract (全平台) |
| 图片处理 | image-rs (格式解码与缩放) |
| 文件监控 | notify 库 (FSEvents / inotify / ReadDirectoryChanges) |
| 并行处理 | Rayon |
| 后端语言 | Rust (edition 2024) |
| 前端构建 | Vite |
| 包管理 | npm / Cargo |

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 0.1.0 | 2026-07 | 初始版本：全文搜索、OCR、多格式支持、实时监控 |

---

*© 2026 Link-Searcher. MIT License.*