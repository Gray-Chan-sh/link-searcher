# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target/
src-tauri/.env
src-tauri/data.db*
src-tauri/*.log
src-tauri/index/
# 本机 Cargo 环境变量（如 sherpa-onnx 预编译库路径）— 勿入库，避免平台绑定
src-tauri/.cargo/config.toml

# Local session data
.omo/
opencode.json

# Agent/tool local state — never commit
.commandcode/
.inkos/
inkos.json

# Python venv for FunASR
src-tauri/models/funasr/.venv/
.env
node_modules/

# Dev tooling scripts: keep only the portable setup helpers, ignore the rest
# (eval corpora, private fixtures).
scripts/*
!scripts/setup-dev.sh
!scripts/setup-dev.ps1
